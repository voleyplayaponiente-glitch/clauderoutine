services:
  conectores-finanzas:
    build: .
    container_name: conectores-finanzas
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      # Las copias viven FUERA del contenedor: si se recrea, no se pierden.
      - ./datos:/datos
